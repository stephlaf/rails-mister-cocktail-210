<%= simple_form_for @cocktail do |f| %>
  <%= f.input :name, label: 'Name, please' %>
  <%= f.input :photo %>
  <%= f.input :photo_cache, as: :hidden %>
  <%= f.submit 'Do it!', class: 'btn btn-sm btn-primary' %>
<% end %>
